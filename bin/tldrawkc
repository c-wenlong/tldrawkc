#!/bin/sh
# Thin shim so the tool runs from inside the submodule, from a global
# `npm link`, or from `npx tldrawkc`. The real entry point is the compiled
# CLI; `npm run build` has to have run at least once.
exec node "$(dirname "$0")/../dist/cli/index.js" "$@"
